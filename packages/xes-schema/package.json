{
  "name": "@agent-hacks/xes-schema",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "pack": "pnpm build && pnpm pack"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
